/**
 *     NotFound component:
 *     For all non-existent routes displays a message "Page not found"
 * @param props empty
 * @returns {JSX.Element} not found message component
 */
export default function NotFound(props) {
    return(
        <h2 className="text-center">Page not found</h2>
    );
}